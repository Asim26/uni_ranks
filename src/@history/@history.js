import * as history from 'history';

// export default history.createBrowserHistory({ basename: '/quantum/uniranks/testing/management' });
export default history.createBrowserHistory({ basename: '/testing/management' });
